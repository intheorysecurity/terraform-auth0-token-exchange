PORT=3000

# Auth0 tenant domain (without https://)
AUTH0_DOMAIN=your-tenant.us.auth0.com

# Terraform outputs / Heroku config vars
AUTH0_API_CLIENT_ID=your_token_exchange_m2m_client_id
AUTH0_API_CLIENT_SECRET=your_token_exchange_m2m_client_secret

# Used to sign the demo "legacy token" JWT (must match Action secret "password")
JSON_SECRET=replace_me_with_random_string

